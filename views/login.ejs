<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" type="text/css" href="css/styles.css">
    <script src="https://kit.fontawesome.com/194f4bae1e.js" crossorigin="anonymous"></script>
    <link rel="shortcut icon" type="image/jpg" href="images/favicon.jpg"/>
    <link rel="stylesheet" href="https://use.typekit.net/hap0koo.css">
    <title>Login</title>
  </head>
  <body>
    <div class="container log_in">
      <div class="header">
        <h1 class="daily">Daily Quote</h1>
        <h1>Log In</h1>
      </div>
      <form id="login-form" class="form" action="/login" method="post">
        <% if(error != 'undefine') { %>
            <small style="color:red"><%= error %></small><br>
        <% } %>
        <% if(success_msg != '') { %>
            <small style="color:red"><%= success_msg %></small><br>
        <% } %>
        <% if(error_msg != '') { %>
            <small style="color:red"><%= error_msg %></small><br>
        <% } %>
        <br>
        <div class="form-settings">
          <label for="username">Username</label>
          <input id="login-username" type="text" name="username">
        </div>
        <div class="form-settings">
          <label for="password">Password</label>
          <input id="login-password" type="password" name="password">
        </div>
        <div class="form-settings">
          <button id="submit-btn" type="submit" name="button">Login</button>
          <br>
          <p>Don't have an account? <a href="/signup">Sign Up</a></p>
        </div>
      </form>
    </div>

  </body>
</html>
